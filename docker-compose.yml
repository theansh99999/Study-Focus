version: "3.9"

services:
  study-focus:
    build: .
    container_name: study_focus_app
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
      - SECRET_KEY=supersecretkey
    restart: unless-stopped
